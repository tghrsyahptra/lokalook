<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ketika checked -->
    <item android:state_checked="true" android:textColor="#FFFFFF" /> <!-- Warna teks putih saat terpilih -->

    <!-- Default (Unchecked) -->
    <item android:textColor="#0BB179" /> <!-- Warna primary (hex) saat tidak terpilih -->
</selector>